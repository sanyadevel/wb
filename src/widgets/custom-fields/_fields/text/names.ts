import { FieldNameMap } from '../../types';
import { Struct } from './types';

export const names: FieldNameMap<Struct> = {
  _: '',
  id: 'id',
  type: 'type',
  value: 'value',
  label: 'label',
};
